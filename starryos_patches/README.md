# StarryOS Reference Files

These files record the StarryOS proc/stat implementation used during validation:

```text
kernel/src/pseudofs/proc.rs
kernel/src/task/stat.rs
```

They are included so the memory-reporting behavior used by the benchmark can be
reviewed and reproduced.
